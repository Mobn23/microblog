apiVersion: v2
name: microblog
description: Helm chart for deploying Microblog (Flask) with MySQL, TLS Ingress via cert-manager, and autoscaling on Kubernetes
type: application
version: 0.1.0
appVersion: "1.0.0-prod"
